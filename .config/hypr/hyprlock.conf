background {
    monitor =
    path = ~/.current_lock_wallpaper
    blur_passes = 3
    blur_size = 3
    vibrancy = 0.1696
}

input-field {
    monitor =
    size = 200, 50
    outline_thickness = 3
    dots_size = 0.33 # Scale of input-field height, 0.2 - 0.8
    dots_spacing = 0.15 # Scale of dots' absolute size, 0.0 - 1.0
    dots_center = true
    dots_rounding = -1 # -1 default circle, -2 follow input-field rounding
    outer_color = rgb(D2C4A0)
    inner_color = rgb(282828)
    font_color = rgb(D2C4A0)
    fade_on_empty = true
    fade_timeout = 1000 # Milliseconds before fade_on_empty is triggered.
    placeholder_text = <i>Input Password...</i> # Text rendered in the input box when it's empty.
    hide_input = false
    rounding = -1 # -1 means complete rounding (circle/oval)
    check_color = rgb(204, 136, 34)
    fail_color = rgb(204, 34, 34) # if authentication failed, changes outer_color and fail message color
    fail_text = <i>$FAIL <b>($ATTEMPTS)</b></i> # can be set to empty
    capslock_color = -1
    numlock_color = -1
    bothlock_color = -1 # when both locks are active. -1 means don't change outer color (same for above)
    invert_numlock = false # change color if numlock is off
    swap_font_color = false # see below
    position = 0, -20
    halign = center
    valign = center
}

label {
    monitor =
    text = cmd[update:1000] echo "$(date "+%A, %B %d, %Y")"
    color = rgb(EBDBB2)
    font_size = 16
    font_family = Cascadia Code # Fira Semibold
    position = 0, 150
    halign = center
    valign = center
    shadow_passes = 5
    shadow_size = 10
}

label {
    monitor =
    text = cmd[update:1000] echo "$TIME"
    color = rgb(EBDBB2)
    font_size = 72
    font_family = Cascadia Code # Fira Semibold
    position = 0, 200
    halign = center
    valign = center
    shadow_passes = 5
    shadow_size = 10
}

label {
    monitor =
    text = $USER
    color = rgb(EBDBB2)
    font_size = 24
    font_family = Cascadia Code # Fira Semibold
    position = 0, 80
    halign = center
    valign = bottom
    shadow_passes = 5
    shadow_size = 10
}

label {
    monitor =
    text = cmd[update:1000] echo "$(playerctl -a metadata --format '{\{artist\}} - {\{markup_escape(title)\}}' | awk '{if (length($0) > 20) print substr($0, 1, 40) "..."; else print}')"
    color = rgb(EBDBB2)
    font_size = 16
    font_family = Cascadia Code # Fira Semibold
    position = 80, 80
    halign = left
    valign = bottom
    shadow_passes = 5
    shadow_size = 10
}
